apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: argocd

bases:
  - ../../base

commonLabels:
  environment: dev
  managed-by: gitops

# Add resource quotas for dev (generous limits)
patchesStrategicMerge:
  - resource-quota.yaml
