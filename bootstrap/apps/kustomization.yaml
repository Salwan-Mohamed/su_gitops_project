apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: argocd

resources:
  - infrastructure.yaml
  - platform-services.yaml
  - monitoring.yaml
  - grafana.yaml
  - awx.yaml

commonLabels:
  managed-by: gitops
