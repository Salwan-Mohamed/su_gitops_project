apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: argocd

resources:
  - infrastructure.yaml
  - platform-services.yaml
  - monitoring.yaml
  - grafana.yaml
  - sample-nginx-dev.yaml
  - sample-nginx-staging.yaml
  - sample-nginx-production.yaml
  - awx-operator.yaml
  - awx.yaml

commonLabels:
  managed-by: gitops
