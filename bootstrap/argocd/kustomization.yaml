apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: argocd

resources:
  - namespace.yaml
  - appproject-infrastructure.yaml
  - appproject-applications.yaml

# Common labels for all resources
commonLabels:
  managed-by: gitops
  component: bootstrap
